#include <iostream>
#include <vector>
#include <string>
using namespace std;

vector <string> Splitstring (string s1,string Delim= " ")
{

    short pos=0;
    short count=0;
    string words;
    vector <string> VWords;
    while((pos=s1.find(Delim))!=s1.npos)
    {
        words=s1.substr(0,pos);
        VWords.push_back(words);
        s1.erase(0,pos+Delim.length());
    }

    if(s1!="")
    {
        VWords.push_back(s1);
    }
    return VWords;

}
string ConvirtStringToSmall (string s1)
{
    for(int i=0;i<s1.length();++i)
    {
        s1[i]=tolower(s1[i]);
    }
    return s1;
}
string JoinString (vector <string> vs,string delim=" ")
{
    string s1;
    for(string &s:vs)
    {
     s1+=s+delim;   
    }
    return s1.substr (0,s1.length()-delim.length());
}
string ReblaceWordInStrungMachCase(string s1,string toreplace,string replaceto,bool machCase=true)
{
  vector <string> VString=Splitstring(s1);
  for(string &s:VString)
  {
      if(machCase)
      {
          if(s==toreplace)
          {
              s=replaceto;
          }
      }
      else
      {
          if(ConvirtStringToSmall(s)==ConvirtStringToSmall(toreplace))
          {
              s=replaceto;
          }
      }
  }
  
  return JoinString (VString);
  
  
  
  
    
}
int main()
{
string txt="welcome to baghdad baghdad is capital of iraq";
string toreplace="baghdad";
string replaceto="Al mansor";
cout<<"orginal text\n\n"<<txt;

cout<<"\n\nReplace whit mach case\n"<<ReblaceWordInStrungMachCase(txt,toreplace,replaceto);
cout<<"\n\nReplace whit not mach case\n"<<x(txt,toreplace,replaceto,false);


    return 0;
}