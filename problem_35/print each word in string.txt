#include <iostream>
#include <string>
using namespace std;
string ReadString ()
{
    string s1;
    cout<<"enter txt\n";
    getline(cin,s1);
    return s1;
}
void printEachWrdInString(string s1)
{
    string space=" ";
    short pos=0;
    string words;
    while((pos=s1.find(space))!=s1.npos)
    {
        words=s1.substr(0,pos);
        cout<<words<<endl;
        s1.erase(0,pos+space.length());
    }
    
    if(s1!="")
    {
        cout<<s1;
    }
}


int main()
{
   
   string s1=ReadString();
   printEachWrdInString(s1);
   
   
   
   
    
    return 0;
}